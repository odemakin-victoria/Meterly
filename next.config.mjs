/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['optiweb.optimusbank.com'], // Add your allowed domains here
  },
};

export default nextConfig;
